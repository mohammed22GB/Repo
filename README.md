# PLUG Frontend App

## Installation

- Clone the repo in your terminal by clicking the green clone or download button at the top right and copyin the url

- In your terminal, type `git clone <URL>` replace URL with the url you copied

- In your terminal, navigate to the directory you just cloned

- Type `npm install` to install all dependencies

- Last, but not least, type `npm start` to run the app locally.

Versions:

- 0.3.9

  - UI Editor upgrade
  - other updates

- 0.4.6
  - Variables Mgt refactoring
  - other updates
